<UserControl x:Class="ClientWPF.Views.Components.PostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:componentviews="clr-namespace:ClientWPF.Views.Components"
             xmlns:componentviewmodels="clr-namespace:ClientWPF.ViewModels.Components"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="800">

	<Grid Background="WhiteSmoke">

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="100" />
			<ColumnDefinition Width="*" MinWidth="320" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="100" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<componentviews:ProfileImageView DataContext="{Binding Profile.Image}" Height="80" Width="80" />

		<StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2">
			<Label Content="{Binding Profile.Name}" Style="{StaticResource SubHeadingLabel}" />
			<TextBlock Text="{Binding Content}" Style="{StaticResource TextBlock}" />
			<ContentPresenter Content="{Binding Image}">
				<ContentPresenter.Resources>
					<DataTemplate DataType="{x:Type componentviewmodels:ImageViewModel}">
						<componentviews:ImageView DataContext="{Binding}" />
					</DataTemplate>
				</ContentPresenter.Resources>
			</ContentPresenter>
			<StackPanel Orientation="Horizontal">
				<Label Content="Last Updated:" Style="{StaticResource FieldLabel}" />
				<Label Content="{Binding TimePosted}" Style="{StaticResource FieldLabel}" />
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>