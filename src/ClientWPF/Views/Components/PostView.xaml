<UserControl x:Class="ClientWPF.Views.Components.PostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:componentviewmodels="clr-namespace:ClientWPF.ViewModels.Components"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="800">

	<UserControl.DataContext>
		<componentviewmodels:PostViewModel />
	</UserControl.DataContext>

	<Grid Background="WhiteSmoke">
		
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="100" />
			<ColumnDefinition Width="*" MinWidth="320" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="100" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Ellipse Width="80" Height="80" Grid.Column="0" Grid.Row="0" Margin="10">
			<Ellipse.Fill>
				<ImageBrush ImageSource="{Binding Avatar}" />
			</Ellipse.Fill>
		</Ellipse>

		<StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2">
			<Label Content="{Binding Name}" Style="{StaticResource SubHeadingLabel}" />
			<TextBlock Text="{Binding Content}"
						   Style="{StaticResource TextBlock}" />
			<StackPanel Orientation="Horizontal">
				<Label Content="Last Updated:" Style="{StaticResource FieldLabel}" />
				<Label Content="{Binding LastUpdated}" Style="{StaticResource FieldLabel}" />
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>