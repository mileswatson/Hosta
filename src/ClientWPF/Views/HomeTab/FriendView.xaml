<UserControl x:Class="ClientWPF.Views.HomeTab.FriendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:componentviews="clr-namespace:ClientWPF.Views.Components"
             mc:Ignorable="d"
			 Width="200" Height="60">
	<Border Background="WhiteSmoke" BorderBrush="DimGray" BorderThickness="2px" CornerRadius="5">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<componentviews:ProfileImageView DataContext="{Binding Image}" Grid.Column="0"
										 Width="40" Height="40" Margin="10" />

			<TextBlock Text="{Binding Name}" Grid.Column="1" TextWrapping="WrapWithOverflow"
			   Style="{StaticResource TextBlock}" FontWeight="Bold" Margin="5,0" FontSize="16" />
		</Grid>
	</Border>
</UserControl>