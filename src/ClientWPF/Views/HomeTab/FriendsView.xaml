<UserControl x:Class="ClientWPF.Views.HomeTab.FriendsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:componentviewmodels="clr-namespace:ClientWPF.ViewModels.Components"
             xmlns:local="clr-namespace:ClientWPF.Views.HomeTab"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<StackPanel Background="White">
		<Label Content="Best Friends" Style="{StaticResource SubHeadingLabel}" />
		<ItemsControl ItemsSource="{Binding BestFriends}">
			<ItemsControl.ItemTemplate>
				<DataTemplate DataType="{x:Type componentviewmodels:ProfileViewModel}">
					<local:FriendView DataContext="{Binding}" Margin="0, 5" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
		<Label Content="Friends" Style="{StaticResource SubHeadingLabel}" />
		<ItemsControl ItemsSource="{Binding Friends}">
			<ItemsControl.ItemTemplate>
				<DataTemplate DataType="{x:Type componentviewmodels:ProfileViewModel}">
					<local:FriendView DataContext="{Binding}" Margin="0, 5" />
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</StackPanel>
</UserControl>